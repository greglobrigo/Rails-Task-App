<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid" style="padding: 0 20vw">
    <a class="navbar-brand" href="#">Binder</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <%=link_to categories_path, class: "nav-link active" do%>
            <li class="nav-item">
              <button class="navbar-shared">
                Dashboard
              </button>
            </li>
          <%end%>       
        <%else%>
          <li class="nav-item">
            <%=link_to root_path, class: "nav-link active" do%>
              <button class="navbar-shared">
                Home
              </button>
            <%end%>
          </li>
        <%end%>
      </ul>
      <div class="d-flex home-links">
        <% if user_signed_in? %>
          <%= link_to edit_user_registration_path, :class => 'nav-link active' do%>
            <button class="navbar-shared">
              Edit Profile
            </button>
          <%end%>
          <%= link_to destroy_user_session_path, method: :delete, :class => 'nav-link active', data: { confirm: 'Are you sure you want to sign out?' } do%>
            <button class="navbar-shared">
              Logout
            </button>
          <%end%>
        <%else%>
          <%=link_to '/users/sign_in', class: "nav-link active" do%>
            <button class="navbar-shared">
              Sign in
            </button>
          <%end%>
          <%=link_to '/users/sign_up', class: "nav-link active" do%>
            <button class="navbar-shared">
              Sign up
            </button>
          <%end%>
        <% end %>
      </div>
    </div>
  </div>
</nav>
