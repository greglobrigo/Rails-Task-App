<div class="add-new-and-edit-category-container">
  <div class="add-new-and-edit-category-img-container">
    <div class="add-new-and-edit-category-img">
      <%= image_tag("new-category.svg", :alt => "blog-img") %>
    </div>
  </div>
  <div class="add-new-and-edit-category-form">
    <h1>New Category</h1>
    <%= form_with(model: @category) do |form| %>
      <% if @category.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>
          <ul>
            <%@category.errors.each do |error| %>
              <li><%= error.full_message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>
      <div class="field">
        <%= form.label :title %>
        <%= form.text_field :title %>
      </div>
      <div class="actions">
        <%= form.submit%>
      </div>
    <% end %>
    <%= link_to '/categories' do%>
      <button class="navigation-button">Back</button>
    <%end%>
  </div>
</div>
